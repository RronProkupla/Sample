<nav class = "navbar navbar-expand-md navbar-dark bg-dark">
  <a href="/" class="navbar-brand"><img src="/images/booklogo.jpg"></a>
  {{#if user}}
  <button class="navbar-toggler" data-toggle="collapse" data-target="#navbarMenu">
    <span class="navbar-toggler-icon"></span>
  </button>
    <div class="collapse navbar-collapse" id="navbarMenu">
      <ul class="navbar-nav ml-auto">
        {{#if user.isAdmin}}
        <li class="nav-item">
          <a href="/book/new" class="nav-link">Add Book</a>
        </li>
        {{/if}}
        <li class="nav-item">
          <a href="/user/logout" class="nav-link">Log Out</a>
        </li>
      </ul>
    </div>
  {{else}}
  <button class="navbar-toggler" data-toggle="collapse" data-target="#navbarMenu">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarMenu">
    <ul class="navbar-nav ml-auto">
      <li class="nav-item">
        <a href="/user/login" class="nav-link">Log In</a>
      </li>
      <li class="nav-item">
        <a href="/user/register" class="nav-link">Register</a>
      </li>
    </ul>
  </div>
  {{/if}}
</nav>
